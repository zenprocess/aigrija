name: "Verificare SMS suspect"
description: "Utilizatorul primeste un SMS suspect de la banca si il verifica pe ai-grija.ro"
steps:
  - action: navigate
    url: "/#/"
  - action: wait
    selector: "[data-testid='checker-textarea']"
    timeout: 15000
  - action: screenshot
    name: homepage
  - action: fill
    selector: "[data-testid='checker-textarea']"
    value: "Contul dvs ING a fost blocat. Accesati urgent: http://ing-verify.com/deblocare"
  - action: click
    selector: "[data-testid='checker-submit-btn']"
  - action: wait
    selector: "[data-testid='checker-action-share']"
    timeout: 30000
  - action: screenshot
    name: verdict-phishing
  - action: assert
    selector: body
    contains: "PHISHING"
  - action: click
    selector: "[data-testid='checker-action-share']"
  - action: screenshot
    name: share-modal
